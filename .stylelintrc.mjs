import baseConfig from '@it-incubator/stylelint-config';

const config = {
    ...baseConfig
};

export default config;

/*   pnpm i @it-incubator/prettier-config   */
/*   pnpm add --save-dev --save-exact prettier  */