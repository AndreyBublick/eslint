import baseConfig from '@it-incubator/prettier-config';

const config = {
    ...baseConfig
};

export default config;

/*   pnpm i @it-incubator/prettier-config   */
/*   pnpm add --save-dev --save-exact prettier  */